import { SchemaValidators } from '../types';

const methods: SchemaValidators[] = ['string', 'required', 'email', 'max', 'min', 'number', 'positive', 'url', 'date', 'integer'];

export default methods;